# Commands to Run Referral AI

You need to run the Backend and Frontend in two separate terminals.

## Terminal 1: Backend
Run these commands in the first terminal:

cd Backend
npm install
npm start

## Terminal 2: Frontend
Open a new terminal window at the root of the project (c:\Users\CharishmaReddy\Downloads\referral-ai) and run:

npm install
npm start
